import 'package:flutter/material.dart';

class InstructorIzinPage extends StatelessWidget {
  const InstructorIzinPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Instructor Izin'),
      ),
      body: const Center(
        child: Text('Instructor Izin Page'),
      ),
    );
  }
}
